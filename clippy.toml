# Product and project names that read like identifiers to `clippy::doc_markdown`
# but are ordinary prose. `..` keeps clippy's own default list.
doc-valid-idents = ["WeftDB", "QuestDB", "InfluxDB", "OpenTelemetry", "FastLanes", ".."]
