[package]
name = "weft-line-protocol"
version.workspace = true
edition = "2021"
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
description = "Vendor-neutral InfluxDB Line Protocol (ILP) parser: text in, neutral records / splimes points out. No client, no network, no vendor dependency."
keywords = ["influxdb", "line-protocol", "parser", "timeseries", "ilp"]
categories = ["parser-implementations", "encoding", "database"]


[lib]
name = "weft_line_protocol"
path = "src/lib.rs"

[dependencies]
bigdecimal = { workspace = true }
chrono = { workspace = true }
splimes = { workspace = true }
